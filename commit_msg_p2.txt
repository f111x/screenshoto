feat: all P2 features — share links, compare/diff, batch, API, AI fix

P2-1: Share links
- Create share endpoint (POST /api/share)
- Public share page (/s/[id])
- One-click copy share URL
- File-based storage (.data/shares/)

P2-2: Compare view + similarity scoring
- Side-by-side comparison (original vs generated)
- AI-powered similarity scoring (GPT-4o Vision)
- Toggle between compare/preview modes

P2-3: Batch conversion
- Multi-file upload with progress queue
- Sequential batch processing
- Per-item status tracking

P2-4: Public API
- REST API endpoint (POST /api/v1/generate)
- API key auth via Bearer token
- Rate limiting (10 req/min per IP)
- Interactive API docs page (/docs)

P2-5: AI fix suggestions
- AI-powered code quality analysis
- Issue display with type labels
- One-click apply suggested fixes

Co-Authored-By: AtomCode (deepseek-v4-flash) <noreply@atomgit.com>
